<?php

namespace Router;

// lazy router implementation

// instead of eagerly adding all paths, it loads only limited subset of paths
// it also allows to split paths into separate files to allow further modularization

class LazyRouter {

    public function __construct() {

    }

}